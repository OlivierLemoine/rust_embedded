[package]
name = "arm"
version = "0.1.0"
authors = ["olivier"]
edition = "2018"

[build-dependencies]
cc = "1.0"

[dependencies]
HAL = { path = "hal" }

[profile.dev]
opt-level = 0
debug = true

panic = 'abort'
target = 'thumbv7em-none-eabihf'
crate-type = ['staticlib']